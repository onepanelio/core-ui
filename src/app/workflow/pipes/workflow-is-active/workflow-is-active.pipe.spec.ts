import { WorkflowIsActivePipe } from './workflow-is-active.pipe';

describe('WorkflowIsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkflowIsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
