import { WorkspacePhaseStatusPipe } from './workspace-phase-status.pipe';

describe('WorkspacePhaseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkspacePhaseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
