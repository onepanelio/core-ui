import { WorkspacePhaseImagePipe } from './workspace-phase-image.pipe';

describe('WorkspacePhaseImagePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkspacePhaseImagePipe();
    expect(pipe).toBeTruthy();
  });
});
