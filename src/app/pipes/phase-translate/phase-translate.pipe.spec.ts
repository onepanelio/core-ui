import { PhaseTranslatePipe } from './phase-translate.pipe';

describe('PhaseTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PhaseTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
