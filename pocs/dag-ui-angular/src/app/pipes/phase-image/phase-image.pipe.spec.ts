import { PhaseImagePipe } from './phase-image.pipe';

describe('PhasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhaseImagePipe();
    expect(pipe).toBeTruthy();
  });
});
